import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const CusFooter = () => {
    return(
        <Footer
            style={{
                textAlign: "center",
            }}
        >
            云销
        </Footer>
    );
};

export default CusFooter;
